spring:
  application:
    name: jooq-testcontainer-flyway-kotlin-spring

  jooq:
    sql-dialect: MySQL
  datasource:
    url: jdbc:tc:mysql:8://test
    username: root
    password: verysecret
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    fail-on-missing-locations: true
